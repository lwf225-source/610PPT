# 610PPT V2 本地界面

V2 是 610PPT 唯一保留的本地网页界面。`v2/server/production.js` 在 5176 端口提供页面，并在本机启动内部生成引擎。

主流程：上传本地文档 → 配置拆页 → 确认逐页文案 → 选择三种 Image2 风格之一 → 确认封面与正文锚点 → 生成和审阅 → 导出整页图 PPTX。

项目任务由本机持久化存储管理。关闭页面或重启服务后可以恢复任务状态；风格未确认时不会开始付费生成。
